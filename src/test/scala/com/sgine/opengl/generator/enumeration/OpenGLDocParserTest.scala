package com.sgine.opengl.generator.enumeration

import org.scalatest.FlatSpec
import org.scalatest.matchers.ShouldMatchers


/**
 * Specification for OpenGLDocParser.
 *
 */
class OpenGLDocParserTest extends FlatSpec with ShouldMatchers {

  
  "OpenGLDocParser" should "produce an empty list for empty input" in (pending)

  "OpenGLDocParser" should "parse a simple enum sequence" in (pending)

}
